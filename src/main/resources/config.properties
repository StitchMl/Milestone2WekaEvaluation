# If true, does hold out preserving the order of instances
preserveOrder=true

# Quota di training per lo holdout (es. 0.8 = 80%)
holdoutRatio=0.8

# (Other existing properties such as dataset.paths, etc.).